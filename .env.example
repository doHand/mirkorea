# =============================================================
# 로컬 개발 환경 (.env 파일로 복사해서 사용)
# =============================================================
SPRING_PROFILES_ACTIVE=dev
PORT=4000

# 데이터베이스 (로컬 Docker PostgreSQL)
DB_HOST=localhost
DB_PORT=5433
DB_USER=wms_user
DB_PASSWORD=CHANGE_ME
DB_NAME=wms_pro

# 인증
JWT_SECRET=wmsPro2026LocalDevJwtSecretKey_9xK2mP7qR4sT8vY6bN3cD5fG1hJ0
JWT_EXPIRATION=86400
JWT_REFRESH_EXPIRATION=2592000

# CORS / API
CORS_ORIGINS=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:4000


# =============================================================
# Railway 프로덕션 환경변수 (Railway 대시보드에서 직접 설정)
# =============================================================

# --- backend 서비스 ---
SPRING_PROFILES_ACTIVE=prod
PORT=4000

DB_HOST=${{Postgres.PGHOST}}
DB_PORT=${{Postgres.PGPORT}}
DB_USER=${{Postgres.PGUSER}}
DB_PASSWORD=${{Postgres.PGPASSWORD}}
DB_NAME=${{Postgres.PGDATABASE}}

JWT_SECRET=<강력한 랜덤 시크릿 32자 이상>
JWT_EXPIRATION=86400
JWT_REFRESH_EXPIRATION=2592000

CORS_ORIGINS=https://wms.mirkorea.kr

# --- frontend 서비스 ---
NEXT_PUBLIC_API_URL=https://<backend-railway-도메인>
